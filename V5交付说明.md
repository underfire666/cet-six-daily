# V5 交付说明：正式开放「阅读」专项

> 分支：`feature/v5-reading`（未合并 main，等待人工验收）
> 状态：V5（待验收）· 2026-09-18
> 前置版本：V4（词汇专项，已收尾）

## 1. 实现内容

V5 唯一核心目标：正式开放「阅读」专项，形成「进入阅读 → 今日任务 → 阅读原文 → 点词查义 → 加入统一生词本 → 答题 → 结算 → 继续下一篇」的完整闭环。

- 阅读入口开放：学习首页阅读卡片由「敬请期待」改为「阅读 · 今日 x/3」。
- 今日阅读任务：每日固定 3 篇（`DAILY_ARTICLE_COUNT=3`），按日期确定性轮换，同一天刷新结果不变。
- 阅读页面：自然排版、舒适行距、支持滚动；底部「开始答题」。
- 点词查义：点击英文单词弹出查词卡片（单词 / 音标 / 中文释义）；未收录词显示「暂未收录释义」提示，不报错。
- 统一生词本：收藏词写入现有「我的生词」数据（`source: "reading"`），与词汇收藏共用一套系统；已收藏去重，重复点击显示「已加入生词」。
- 答题：复用 V2 通用答题状态机与 `QuestionRenderer / AnswerFeedback / LessonProgress / AiHint / LessonExitDialog`；第一次答错允许重试、第二次答错显示答案与解析、支持展开详细解析、支持 AI 提示；答题中可「查看原文」且不丢失答题状态。
- 完成结算：正确率、本篇答对、独立答对、需加强、本篇生词、+XP；主按钮「继续下一篇 / 今天再读一篇」，次级「返回阅读首页」。
- 每日任务状态：0/3、1/3、3/3 ✓（今日任务完成）、今日额外阅读 X 篇；完成 3 篇后仍可继续额外阅读，永不显示「5/3」。
- 刷新恢复：阅读进度、答题状态、收藏、XP 全部本地持久化，刷新后恢复到原位置。
- XP：复用现有 XP 体系与账本；完成一篇文章 = 基础分 + 表现分；重复阅读不重复发放首次奖励。

## 2. 主要文件

### 新增

| 文件 | 说明 |
| --- | --- |
| `src/types/reading.ts` | ReadingArticle / ReadingSession / ReadingStore 等类型（PassageQuestion 命名避开 V2 的 ReadingQuestion 冲突） |
| `src/data/mockReading.ts` | 6 篇原创 B2 英文文章（250–450 词，每篇 4 题、7 个查词条目），主题覆盖科技、环境、社会、教育、健康、文化 |
| `src/data/readingVocabulary.ts` | 由文章词汇生成统一 `Word[]`（id=`rw_<词>`、`source: "reading"`） |
| `src/lib/reading/questions.ts` | 文章 → 通用 LessonDefinition（题 id 加 `rq:` 前缀，module: "reading"） |
| `src/lib/reading/session.ts` | 阅读 Session 三阶段（reading→quiz→complete）与 actions（start_quiz / collect_word / lesson） |
| `src/lib/reading/xp.ts` | 阅读 XP：基础 15 + 每题表现分；账本 `reading:first:<id>`、`reading:day:<date>:<id>` |
| `src/lib/reading/store.ts` | 每日 3 篇计划、startReading / updateReading / readingDayStats |
| `src/lib/reading/storage.ts` | LocalStorage（键 `cet-daily:v1:reading`），版本化校验 + 坏记录隔离 + 内存降级 |
| `src/components/reading/ReadingProvider.tsx` | 阅读全局状态（load / save / start / dispatch / plan / stats） |
| `src/components/reading/ReadingHome.tsx` | 阅读首页（今日任务、进度条、开始/继续、额外阅读、生词入口） |
| `src/components/reading/ReadingPassage.tsx` | 原文展示 + 分词点词 |
| `src/components/reading/WordLookupSheet.tsx` | 查词 Bottom Sheet（词义 + 加入生词 + 去重状态） |
| `src/components/reading/ReadingSessionPlayer.tsx` | 阅读 → 答题 → 复测流程（复用通用答题组件 + 查看原文） |
| `src/components/reading/ReadingComplete.tsx` | 完成结算页 |
| `src/components/reading/reading.css` | 阅读样式 |
| `src/app/practice/reading/layout.tsx` / `page.tsx` | 阅读首页路由 |
| `src/app/practice/reading/session/[id]/page.tsx` | 阅读/答题页路由 |
| `src/app/practice/reading/complete/[id]/page.tsx` | 完成页路由 |
| `tests/reading.test.ts` | 22 项阅读自动测试 |

### 修改（最小改动，V1–V4 回归通过）

| 文件 | 改动 |
| --- | --- |
| `src/data/mockVocabulary.ts` | `wordById` 扩展可检索阅读词（统一生词本关键） |
| `src/lib/vocabulary/store.ts` | 新增 `addWordToWordbook`（已收藏返回 `added=false` 去重） |
| `src/components/vocabulary/VocabularyProvider.tsx` | 暴露 `addWordbookWord` |
| `src/components/lesson/LessonProgress.tsx` | 增加可选 `label` prop（向后兼容） |
| `src/components/lesson/lesson.css` | 新增 `.exercise-header-label` |
| `src/components/lesson/QuestionRenderer.tsx` | module==="reading" 显示「阅读理解」眉标 |
| `src/components/PracticeModules.tsx` | 阅读入口显示「今日 x/3」 |
| `src/app/globals.css` | `.practice-module-status` 样式 |
| `src/components/AppShell.tsx` | reading 的 session/complete 视为 exercise，隐藏底部导航 |
| `src/app/layout.tsx` | 挂载 ReadingProvider |

## 3. 路由

| 路由 | 说明 |
| --- | --- |
| `/practice/reading` | 阅读首页（今日任务 / 额外阅读 / 生词入口） |
| `/practice/reading/session/[id]` | 阅读原文 → 答题 → 复测 |
| `/practice/reading/complete/[id]` | 完成结算 |

## 4. 数据结构

### ReadingArticle

```ts
{
  id: string;               // 如 r-ai-screening
  title: string;
  passage: string;          // 250~450 英文词
  difficulty: "easy"|"normal"|"hard";  // 当前默认 normal，预留
  estimatedMinutes: number;
  questions: PassageQuestion[];   // 3~5 题四选一
  vocabulary: Record<string, ReadingLookupEntry>;  // key = 原文中的词形
  sourceType: "mock";
}
```

### ReadingSession（schemaVersion=1）

```ts
{
  schemaVersion: 1;
  id: string;
  mode: "daily" | "extra";
  planDate: string;          // 每日任务的计划日；额外阅读为创建当日
  articleId: string;
  phase: "reading" | "quiz" | "complete";
  readingCompleted: boolean;
  collectedWordIds: string[];   // 本篇新加入统一生词本的词 id
  startedAt: string;
  completedAt?: string;
  applied: boolean;
  rewardXp?: number;
  lesson: LessonSession;     // 复用 V2 通用答题状态机
}
```

### ReadingStore（schemaVersion=1）

```ts
{
  schemaVersion: 1;
  daily: Record<string, ReadingDailyProgress>;  // date → { articleIds, completedArticleIds, activeSessionId }
  sessions: Record<string, ReadingSession>;
  xpLedger: Record<string, number>;
}
```

## 5. LocalStorage

- 阅读存储键：`cet-daily:v1:reading`（独立版本化，不影响既有键）。
- 词汇册沿用 `cet-daily:v3:vocabulary`；阅读收藏写入同一词汇库，`source: "reading"`。
- 载入时校验 schemaVersion 与结构，坏记录隔离并提示；存储不可用时提示并使用内存，不让整个网站崩溃。
- 数据仅属于当前浏览器与站点地址，不支持跨设备同步 / 多标签并发合并。

## 6. 关键逻辑

- **今日 3 篇**：`pickDailyArticles(date)` 按 `dayNumber % 6` 确定性取 3 篇；`planFor` 读取/生成当日计划；完成进度为 `completedArticleIds`。
- **额外阅读**：仅当日任务完成后可开始；优先选「非当日计划且今日未完成」的文章；Mock 数据不足时回退到非计划文章重复练习，按重复 XP 处理（v5 §27）。
- **答题**：复用 V2 状态机（answering → retry → feedback → review_intro → complete）；两次答错进入末尾复测；AI 提示与解析沿用。
- **生词**：阅读词 id=`rw_<词>`，`wordById` 统一检索；`addWordToWordbook` 已收藏返回 `added=false`；完成页统计 `collectedWordIds` 本篇新词。
- **XP**：基础 15 + 每题表现分（首轮答对 2 / 二次答对或 AI 提示后答对 1 / 其余 0）；账本 `reading:first:<articleId>` 与 `reading:day:<date>:<articleId>`；首次 = 基础 + 表现分，跨日重复仅表现分，同日重复同篇 +0（防刷分）。
- **日期**：任务按计划日归属（跨日完成仍记入计划日）；额外阅读与 XP 按完成时 Asia/Shanghai 日期归属。
- **刷新恢复**：每次操作写入 LocalStorage，Provider 启动时载入；未完成 Session 恢复为「继续阅读」并回到原题。

## 7. 测试与验证

- 自动测试：**51 / 51 通过**（29 项 V1–V4 存量 + 22 项新增阅读）。
- 阅读新增 22 项覆盖：每日任务生成与稳定性、Session 创建/恢复、start_quiz、首轮答对、一错再对、两次答错进复测、完成文章、完成 3/3、额外阅读不可提前开始/剩余文章选择、额外统计归属、XP 首次 / 同日重复 +0 / 跨日仅表现分、生词 source=reading 与去重、统一 wordById 检索与持久化、存储 save/load 往返、坏记录隔离、跨日完成归属、数据质量（250–450 词 / 3–5 题 / 查词条目真实出现）、通用校验兼容 review、生词计数去重。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- dev 冒烟：`/`、`/practice/reading`、`/practice/reading/session/[id]`、`/practice/reading/complete/[id]`、`/practice/vocabulary`、`/me` 均 200；`/practice/reading` 页面正常渲染「今日 / 阅读 / 篇」内容，无编译报错。
- 375/390/430px 与桌面布局：延续现有移动端优先样式与固定底部导航规则，未做布局重构。

## 8. 已知限制

- 6 篇 Mock 文章当日全部读完（3 每日 + 3 额外）后，额外阅读回退到非计划文章重复练习，同日重复同篇 +0 XP；按 v5 §27 的 Mock 数据不足策略处理。
- 阅读词库仅覆盖每篇 7 个核心词；未收录词显示「暂未收录释义」。
- 未做真实题库 / 词典 API / AI / 后端；真实内容后续只需替换数据层。
- 数据仅限当前浏览器；跨设备同步、多标签并发合并不在本版本范围。

## 9. 后续扩展点

- 难度自适应（easy/normal/hard 已预留）与阅读统计（每题耗时、整篇耗时数据已预留）。
- 真实 CET-6 题库接入（替换 `mockReading.ts` 数据源即可）。
- 「我的生词」页面展示来源（`source` 已保存）。
- 听力 / 翻译 / 写作专项（V5 未开发，等待用户指令）。

## 10. 对 V1–V4 核心逻辑的改动说明

仅做了 3 处必要的向后兼容小改动，未重写任何核心逻辑：

1. `wordById`（mockVocabulary）扩展可检索阅读词 —— 统一生词本要求阅读词与词汇词共用检索入口；词汇词检索行为不变。
2. `lib/vocabulary/store.ts` 新增 `addWordToWordbook` —— 纯新增函数，原有 `addWord` 等接口不变。
3. `LessonProgress` 增加可选 `label` prop、`QuestionRenderer` 按 module 显示眉标 —— 均带默认值/条件分支，V1–V4 用法不变。

V1–V4 的 29 项测试全部继续通过，确认未破坏既有功能。
