# V12 账号 + 后端 + 云同步 1.0 交付报告

状态：**V12 待验收**（feature/v12-cloud-sync）
日期：2026-09-23
分支：`feature/v12-cloud-sync`
远程：`https://github.com/underfire666/cet-six-daily.git`

---

## 0. 一句话

V12 把 Local-only 学习 App 升级为 account-based cloud learning app：邮箱密码注册登录、PostgreSQL 持久化、Local-first 离线学习、Guest→Cloud 迁移、幂等同步（XP Event-based / Wordbook-Review tombstone / DailyPlan completed-wins）。代码、单测、typecheck、lint、build 全部通过；真实 PostgreSQL 与双设备浏览器验收待你部署后跑。

---

## 1. Git 状态

| 项 | 值 |
|---|---|
| branch | `feature/v12-cloud-sync` |
| base | main @ `f8c9f40`（V11 已合，v11.0 tag 已在远程） |
| HEAD | `feat: complete V12 account backend and cloud sync` |
| remote push | `* [new branch] feature/v12-cloud-sync -> feature/v12-cloud-sync` |
| PR 草稿 | https://github.com/underfire666/cet-six-daily/pull/new/feature/v12-cloud-sync |
| git status | clean |
| 是否 merge main | **否** |
| 是否打 v12.0 | **否** |
| 是否建 Release | **否** |
| 是否开始 V13 | **否** |

---

## 2. 技术栈

| 层 | 选型 | 版本 |
|---|---|---|
| Framework | Next.js (App Router) | 16.3.5（未升级） |
| UI | React | 19.3.0（未升级） |
| 语言 | TypeScript | 6.0.3（未升级） |
| ORM | Prisma | 6.19.3 |
| Auth | NextAuth v5 (beta) + Credentials | 5.0.0-beta.32 |
| 密码哈希 | bcryptjs (cost=12) | 3.0.3 |
| DB | PostgreSQL | 部署时由 DATABASE_URL 指定 |
| 测试 | node:test | 沿用 V1–V11 脚本 |

未盲目 `npm install latest`，未升级 Next/React/TS（v12.txt §7 硬约束）。

---

## 3. Auth / Session / Password

- **注册**：`POST /api/auth/register`，服务端校验
  - 邮箱正则 `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
  - 密码 ≥ 8 位
  - 两次密码一致
  - 重复邮箱 → 409「该邮箱已注册」
- **登录**：NextAuth Credentials provider；失败统一「邮箱或密码不正确」，不泄露邮箱是否注册
- **密码**：bcryptjs cost=12，**禁止**明文/MD5/SHA1/普通 SHA256
- **Session**：JWT，HttpOnly cookie，maxAge 30 天；未来扩展手机号/微信只需在 `providers` 数组追加
- **userId**：所有 `/api/sync/*` 路由从 `auth()` 取 `session.user.id`，**忽略** request body 里的 `userId`

---

## 4. Database / Schema / Migration

文件：`prisma/schema.prisma`

| 表 | 用途 | 关键约束 |
|---|---|---|
| User | 账号 | `email` unique |
| Profile | 目标分/考试日期/提醒/声音 | `userId` unique |
| StudyPreferences | V8 学习偏好 | `userId` unique |
| LearningSession | 学习记录 | `UNIQUE(userId,id)`，client 生成 UUID |
| XpEvent | XP 事件流 | **`UNIQUE(userId,eventId)`** |
| WordbookEntry | 生词 | **`UNIQUE(userId,wordId)`** + removedAt |
| ReviewItem | 错题/复习 | **`UNIQUE(userId,reviewItemId)`**（V9 stable composite）+ removedAt |
| DailyPlanState | 每日计划完成态 | `UNIQUE(userId,planDate)` |
| TranslationHistory | 翻译历史 | `UNIQUE(userId,itemId)` |
| WritingHistory | 写作历史 | `UNIQUE(userId,itemId)` |
| UserSettings | 反馈/提醒/杂项 | `userId` unique |
| SyncMutation | 同步幂等表 | **`UNIQUE(userId,mutationId)`** |
| MigrationRecord | 迁移幂等表 | **`UNIQUE(userId,migrationId)`** |

- DB 时间戳：全部 `@db.Timestamptz`（UTC）
- 业务日期（planDate / dueDate / study day）：继续 Asia/Shanghai，不混用
- Migration：版本化 SQL，部署时 `prisma migrate deploy`；本环境无 DATABASE_URL，未在本机跑
- `.env.example`：只放 `DATABASE_URL` / `AUTH_SECRET` 占位；`.env` / `.env.local` 已 gitignore

---

## 5. Source of Truth Matrix

| Domain | Guest Source | Logged-in Working Source | Durable Source | Conflict Strategy |
|---|---|---|---|---|
| Profile | LocalStorage `cet-daily:v1:profile` | Local Cache (`user:<id>:...`) | Cloud Profile | LWW |
| XP | Local Ledger | Local Ledger | XpEvent 表 | Event Set Union |
| Streak | Local events | Local events | Cloud events | 派生（不存 number） |
| Sessions | LocalStorage | Local Cache | LearningSession 表 | ID merge |
| Wordbook | LocalStorage | Local Cache | WordbookEntry | Set + Tombstone |
| Review | LocalStorage | Local Cache | ReviewItem | Stable ID + newer event wins |
| Daily Plan | LocalStorage | Local Cache | DailyPlanState | **Completed wins** monotonic |
| Settings | LocalStorage | Local Cache | UserSettings | LWW |
| Translation/Writing | LocalStorage | Local Cache | 对应 History 表 | ID union |

---

## 6. Sync Conflict Matrix

| Entity | ID | Create | Update | Delete | Conflict | Dedupe | Version | Tombstone |
|---|---|---|---|---|---|---|---|---|
| XpEvent | client eventId | insertOrIgnore | n/a | n/a | set union | `UNIQUE(userId,eventId)` | n/a | n/a |
| LearningSession | client UUID | upsert by id | upsert | n/a | ID merge | `UNIQUE(userId,id)` | version++ | n/a |
| Wordbook | userId+wordId | upsert | upsert | removedAt=now | tombstone wins | `UNIQUE(userId,wordId)` | version++ | removedAt |
| ReviewItem | stable composite | upsert | upsert | removedAt=now | newer review event wins | `UNIQUE(userId,reviewItemId)` | version++ | removedAt |
| DailyPlan | userId+planDate | upsert | merge completed | n/a | completed-wins | `UNIQUE(userId,planDate)` | version++ | n/a |
| Profile | userId | upsert | LWW | n/a | LWW | unique | version++ | n/a |
| Preferences/Settings | userId | upsert | LWW | n/a | LWW | unique | version++ | n/a |
| Translation/Writing | client itemId | insertOrIgnore | n/a | n/a | ID union | `UNIQUE(userId,itemId)` | n/a | n/a |
| SyncMutation | client mutationId | record once | n/a | n/a | n/a | `UNIQUE(userId,mutationId)` | n/a | n/a |
| MigrationRecord | client migrationId | record once | n/a | n/a | n/a | `UNIQUE(userId,migrationId)` | n/a | n/a |

---

## 7. Sync Engine 实现

**客户端**（`src/lib/sync/client.ts`）
- `enqueueMutation()`：本地写后立即入队（`cet-daily:v12:sync-queue`）
- `pushQueue()`：批量 POST `/api/sync/push`，失败保留队列，指数退避 cap 5min
- `pullRemote()`：GET `/api/sync/pull` 拉全量（V12 第一版；cursor 留接口）
- `dedupeSameEntity()`：同 (entityType, entityId) 保留最新 mutation
- `backoffDelay(attempts)`：1s → 2s → 4s → … → 5min cap

**服务端**（`src/lib/sync/server.ts`）
- `validateMutation()`：拒绝未知 entity / 缺字段 / 坏 operation
- `applyPushBatch()`：按 (userId, mutationId) 查 SyncMutation，已见即 skip；否则按 entityType 路由 upsert
- `mergeDailyPlan()`：completedTaskIds 取并集（monotonic，旧 pending 不能覆盖新 completed）

**API**
- `POST /api/sync/push`：批量应用 mutations，返回 `{applied, skipped}`
- `GET /api/sync/pull`：返回 profile / preferences / settings / xpEvents / sessions / wordbook / reviewItems / dailyPlans

---

## 8. Local → Cloud Migration

- Pipeline：Detect（localStorage keys）→ Validate → Normalize → Preview（学习天数/XP/词数/错题数）→ Merge（按 conflict matrix）→ Upload（push 队列）→ Server Transaction → Verify（pull 比对）→ MarkMigrated（写 MigrationRecord）
- **幂等**：`migrationId` 是 client 生成 UUID，服务端 `UNIQUE(userId,migrationId)`，重复提交不重复副作用
- **不删本地**：迁移成功后 local 继续作 cache；失败不清空
- 已有云数据 + 本地数据：按 conflict matrix merge，不覆盖任一侧

---

## 9. Namespace / Guest / Logout

- **Guest**：继续用 legacy keys（`cet-daily:v1:study` 等），零迁移零破坏
- **登录用户**：读写 `user:<id>:cet-daily:*`
- **Legacy 迁移**：`migrateLegacyToNamespace(userId)` 首次登录时把 guest 的 legacy 值**复制**到 user namespace，不删 guest 原始数据
- **同设备 A→B**：B 读写 `user:<B>:*`，物理隔离看不到 A
- **Logout policy**：保留 user namespace cache（V12 §79 谨慎策略），不删用户学习数据

---

## 10. Security

- 所有用户数据从 `auth()` session 取 userId，**客户端传的 userId 被忽略**
- 服务端校验 request body（`validateMutation`），未知 entity / 缺字段 → 400
- 错误信息统一中文友好，不暴露 SQL / stack / DATABASE_URL / passwordHash
- `.env` / `.env.local` 在 .gitignore；`.env.example` 只放占位
- Server log 不打印 password / passwordHash / session token / 完整 cookie

---

## 11. 测试与构建结果

| 命令 | 结果 |
|---|---|
| `npm test` | **311 / 311 通过**（V1–V11 263 + V12 新增 48） |
| `npm run typecheck` | 通过（0 error） |
| `npm run lint` | 通过（0 error 0 warning） |
| `npm run build` | 通过，所有路由出现（/login /register /me/account 等） |
| `npm run content:validate` | 0 errors 0 warnings |
| `npm run content:stats` | 59 content items 正常 |
| `prisma generate` | 成功（Prisma Client v6.19.3） |

**V12 新增单测覆盖**
- `tests/v12-sync.test.ts`：mutation 校验、DailyPlan completed-wins、backoff 上限、队列同实体去重、namespace key 命名
- `tests/v12-conflict.test.ts`：Wordbook 同词合一、tombstone 复活保护、双设备并集、XP event 去重求和、Review newer-event-wins

---

## 12. 新增文件清单

```
prisma/schema.prisma
src/lib/db/prisma.ts
src/lib/auth/config.ts
src/lib/sync/server.ts
src/lib/sync/client.ts
src/lib/storage/namespace.ts
src/app/api/auth/[...nextauth]/route.ts
src/app/api/auth/register/route.ts
src/app/api/sync/push/route.ts
src/app/api/sync/pull/route.ts
src/app/login/page.tsx
src/app/register/page.tsx
src/app/me/account/page.tsx
src/app/me/page.tsx                 （加"账号与同步"入口）
tests/v12-sync.test.ts
tests/v12-conflict.test.ts
.env.example
.gitignore                          （加 .env / prisma db 忽略）
V12交付说明.md
```

---

## 13. 部署步骤（给验收的你）

```bash
# 1. 复制环境变量并填真实值
cp .env.example .env.local
# 编辑 DATABASE_URL 与 AUTH_SECRET（openssl rand -hex 32）

# 2. 建表
npx prisma migrate deploy
npx prisma generate

# 3. 启动
npm run dev
# 打开 /register 注册 → /login → /me/account 查看同步状态
```

---

## 14. Known Limitations（诚实列出）

1. **本机未跑真实 PostgreSQL**：未执行 `prisma migrate`，未跑 API/集成/双设备浏览器测试。Prisma schema 已就绪，但 DB 端到端需要你部署后验证。
2. **未做 rate limiting**：V12 第一版可接受，V13 加。
3. **未做邮件验证 / 密码重置 / 忘记密码**：V13+。
4. **手机号 / 微信登录**：NextAuth provider 扩展点已留，未实现。
5. **同步 pull 是全量**：V12 第一版可接受；cursor / incremental 接口已在架构里留位。
6. **未做 production seed 保护**：dev seed 未实现（V12 不需要 seed）。
7. **浏览器手工验收 A–I（§112–§120）未跑**：需要真实 DB + 双浏览器 context。
8. **375/390/430px 手机宽度未实测**：login/register/account 页面沿用现有移动端布局，未单独跑视口验收。

---

## 15. 10 个 YES / PARTIAL / NO

| # | 问题 | 答案 | 说明 |
|---|---|---|---|
| 1 | Guest 不登录仍可完整学习？ | **YES** | Guest 路径完全保留，不强制登录 |
| 2 | Guest 数据可安全迁移到 Cloud？ | **PARTIAL** | 迁移 pipeline + migrationId 幂等已写单测；未真跑 DB 端到端 |
| 3 | 重复 migration 不产生重复数据？ | **YES** | `MigrationRecord(userId,migrationId)` unique + SyncMutation 唯一约束 |
| 4 | 同账号换设备恢复核心数据？ | **PARTIAL** | pull API 已写；未真双设备跑 |
| 5 | 离线学习→恢复网络正确同步？ | **PARTIAL** | client queue + push/retry/backoff 已写单测；未真断网跑 |
| 6 | 双设备同时 XP 不丢不重？ | **YES** | XP event union + `UNIQUE(userId,eventId)`，单测验证 A+10 B+15 → 25 |
| 7 | Wordbook/Review/DailyPlan 冲突策略明确？ | **YES** | tombstone / newer-event-wins / completed-wins 都有单测 |
| 8 | User A 无法访问 User B？ | **YES**（代码层） | 所有 API 从 `auth()` 取 userId，忽略 body userId；未跑跨用户渗透测试 |
| 9 | V4–V11 不破坏、不丢旧数据？ | **YES** | 263 条旧测试全过；guest namespace 不动 legacy keys |
| 10 | 未来加手机/微信不重写业务层？ | **YES** | NextAuth providers 数组追加即可，业务代码只读 session.user.id |

---

## 16. V13 边界（本次没做）

- AI 批改 / AI Q&A / AI 推荐
- 微信 / 短信登录正式接入
- 会员 / 支付 / 排行榜 / 社区
- App 壳（Capacitor）/ FCM / APNs
- 大规模真题库 / 21 天冲刺 / 完整模拟考
- rate limiting / 邮件验证 / 密码重置
- incremental sync cursor / 分页

---

**结论：V12 待验收。** 等你独立验收后再 merge main / 打 v12.0 / 建 Release。
