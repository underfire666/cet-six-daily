# V12 独立验收审计报告

审计模式：READ-ONLY AUDIT
审计日期：2026-09-23
审计对象：`feature/v12-cloud-sync` @ `c126a48`

---

## 1. 最终结论

**V12 NEEDS FIX / BLOCKED — 不允许 merge main、不打 v12.0、不建 Release。**

核心理由：
1. **没有版本化 Prisma Migration**（P1）—— 只有 `schema.prisma`，没有 `prisma/migrations/*/migration.sql`，`prisma migrate deploy` 无东西可跑。
2. **没有真实 PostgreSQL E2E 环境**（本机无 Docker / psql / PG 服务）—— 按 §6/§85，没有真实 PG 跑通注册→登录→sync→跨用户隔离，就不能判"可以验收"。
3. **Sync Engine 是死代码骨架，未接入任何业务 Provider**（P1）—— `enqueueMutation` / `dedupeSameEntity` / `migrateLegacyToNamespace` 全仓库零调用；用户做题/收藏/复习的本地变更不会自动入队，"立即同步"按钮 push 的是空队列。
4. **Local→Cloud Migration Pipeline 未实现**（P1）—— 只有 `pushQueue` 调用，没有 Detect/Validate/Normalize/Preview/Merge/Transaction/Verify/MarkMigrated，也没有 migration preview UI。
5. **没有 Server Transaction**（P1）—— `prisma.$transaction` 零调用，批量 push 中途失败会半成功。
6. **XP amount 无上限校验**（P1）—— 任何登录用户可传 `amount=999999999` 给自己加任意 XP。
7. **登录/注册完全无 rate limit**（P1）—— 暴力破解/撞库无任何防护。

---

## 2. Git

| 项 | 值 |
|---|---|
| branch | `feature/v12-cloud-sync` |
| local HEAD | `c126a48317f88db1bae8c635296e0eb6081d59d6` |
| remote HEAD | `c126a48317f88db1bae8c635296e0eb6081d59d6`（**一致**） |
| working tree | 仅 `?? V12独立验收审计报告.md`（本报告，未 commit，按 READ-ONLY 要求） |
| main HEAD | `b08e013`（V11 已固化，docs: mark V11 released as v11.0） |
| v11.0 tag | 存在 |
| v12.0 tag | **不存在** ✓ |
| 未 merge main | ✓ |

---

## 3. PostgreSQL / Prisma / Migration

| 项 | 结果 |
|---|---|
| Docker | **未安装**（`docker` 命令不存在） |
| 本地 PostgreSQL | **未安装**（无 `psql`，无 postgresql 服务） |
| `prisma/schema.prisma` | 存在，`prisma validate` 通过（fake DATABASE_URL） |
| `prisma/migrations/` | **不存在** |
| `git ls-files prisma` | 只有 `schema.prisma`，**没有 migration.sql** |
| `prisma migrate deploy` | **未执行，也无 migration 可执行** |
| 真实 DB 表创建 | **未验证** |
| 空库 fresh migrate | **未验证** |
| 幂等二次 migrate | **未验证** |

**P1**：交付报告声称"版本化 Migration，部署时 `prisma migrate deploy`"，但仓库里根本没有 migration 文件。部署者按文档操作会得到"没有 migration 可应用"。必须先 `prisma migrate dev --name init` 生成并提交 SQL。

---

## 4. Auth

| 项 | 结果 |
|---|---|
| 注册 route | `POST /api/auth/register` 存在，服务端校验邮箱/密码长度/确认一致 |
| 重复邮箱 | 409，DB `User.email` unique 约束设计上存在 |
| 密码哈希 | bcryptjs cost=12，非明文 ✓ |
| 登录 | NextAuth Credentials，失败统一"邮箱或密码不正确" |
| AUTH_SECRET | 从 `process.env.AUTH_SECRET` 读，**未硬编码** ✓ |
| Cookie HttpOnly | NextAuth v5 默认 HttpOnly ✓（未显式配 `cookies`） |
| Cookie Secure | **未显式配置**（NextAuth v5 production 默认 Secure，dev 不 Secure；代码无显式 `useSecureCookies`） |
| 真实注册→登录 E2E | **未跑**（无 DB） |
| 错误密码 vs 不存在邮箱 | 代码层返回同一句，但未真实 HTTP 验证 |
| **Rate limiting** | **完全没有**（register/login 无 throttle/abstraction） |

---

## 5. Authorization / User Isolation

| 项 | 结果 |
|---|---|
| userId 来源 | `/api/sync/push` 与 `/api/sync/pull` 都从 `auth().user.id` 取 ✓ |
| 忽略 body userId | `applyPushBatch` 只用函数参数 `userId`，不读 body.userId ✓ |
| 未登录访问 | 401 ✓（代码层） |
| 跨用户 A→B 真实 HTTP 测试 | **未跑**（无 DB、无 server） |
| client-supplied userId 注入测试 | **未跑** |
| XP amount 任意注入 | **未防**：`amount: Number(p.amount ?? 0)` 无上限、无 source 白名单 — 登录用户可自加任意 XP |

---

## 6. Guest Mode

| 项 | 结果 |
|---|---|
| 代码层 | 未登录走 legacy `cet-daily:*` keys，零改动 |
| Guest 真实浏览器学习 | **未跑**（无 dev server + 无 DB） |
| V4–V11 单测回归 | 263 条旧测试全过 ✓ |

---

## 7. Local→Cloud Migration

| 项 | 结果 |
|---|---|
| `migrateLegacyToNamespace(userId)` | **已定义但全仓库零调用** |
| Migration Preview UI | **不存在**（/me/account 只有"立即同步"和"把本机学习记录合并到账号"按钮） |
| Detect/Validate/Normalize/Preview/Merge/Upload/Transaction/Verify/MarkMigrated | **代码不存在** |
| Server Transaction（`prisma.$transaction`） | **零调用** |
| MigrationRecord 写库 | route 里有 upsert 分支，但没有任何客户端逻辑去生成 migrationId 并调用 |
| 同一份 Guest 数据被自动复制给多个账号的风险 | 当前因为 migrate 函数从未被调用，**反而没发生** — 但这是因为功能没接，不是因为设计对 |
| 取消 migration | 无 UI，无从测起 |

**P1**：交付报告把 Migration Pipeline 写得很完整，实际代码只是一个 never-called 的 helper + 一个空 push 按钮。

---

## 8. Sync Engine

| 项 | 结果 |
|---|---|
| `enqueueMutation` | 已定义，**业务代码零调用** |
| `dedupeSameEntity` | 已定义，**零调用** |
| `loadQueue`/`saveQueue` | 只被 `pushQueue` 自己用 |
| 服务端 `applyPushBatch` | 实现完整，含 SyncMutation 幂等表 |
| 服务端 `validateMutation` | 实现完整 |
| 业务 Provider 接入 | **零接入**（Vocabulary/Reading/Listening/Review/DailyPlan/Profile 都不调 enqueue） |
| XP Event 幂等 | DB 层 `UNIQUE(userId,eventId)` 设计在 ✓ |
| 服务端批量原子性 | **无 transaction**，mutation 之间失败会半成功 |
| 并发同 mutationId 竞态 | 代码是 `findUnique` 再 `create`，并发下有 race（unique 冲突会 500，不是 409） |

**dedupeSameEntity 风险（§40/§55）**：函数从未被调用，所以当前不会丢数据；但一旦按设计接上，对 DailyPlan completedTaskIds 这种**累积型** entity，"只保留最新一条 mutation" 会把另一个设备的完成记录丢掉。服务端 `mergeDailyPlan` 是并集，能补回来，但前提是两条 mutation 都到 server —— 客户端先 dedupe 掉一条就丢了。**设计缺陷，待修。**

---

## 9. Offline / Reconnect

- 客户端 queue 持久化在 `localStorage`（V12 第一版可接受）
- `pushQueue` 失败保留队列 + backoff cap 5min
- 真实离线→恢复网络浏览器测试：**未跑**
- 无 `navigator.onLine` 自动重连监听（只有手动点"立即同步"）

---

## 10. Multi-device

- Browser context A/B 隔离：**未跑**（无 dev server、无 DB）
- A→B 数据恢复：**未跑**
- 双设备离线 XP 求和：只有纯函数单测（`tests/v12-conflict.test.ts` 里 mock 求和=25），**未真实 DB 验证**
- Wordbook union/dedupe/tombstone：同上，纯单测
- DailyPlan completed-wins：纯函数单测 + 服务端 `mergeDailyPlan`，未真实 DB 跑

---

## 11. XP

| 项 | 结果 |
|---|---|
| Event-based | schema 有 XpEvent 表 ✓ |
| UNIQUE(userId,eventId) | ✓ |
| amount 上限 | **无校验** — P1 任意注入 |
| source 白名单 | **无校验** |
| 重复 eventId 去重 | 服务端 `update: {}` 即 no-op ✓ |
| 真实 DB 双设备 XP=25 | **未跑** |

---

## 12. Wordbook / Tombstone

| 项 | 结果 |
|---|---|
| `UNIQUE(userId,wordId)` | ✓ |
| `removedAt` tombstone | ✓ |
| 服务端 upsert 时 removedAt 处理 | ✓（remove 操作写 removedAt） |
| 重新激活（re-add after delete） | 当前 upsert 会把 removedAt 清回 null — 允许重新激活 ✓ |
| 真实 B 同步后不复活 | 纯单测验证逻辑，未真实 DB |

---

## 13. Review

- ReviewItem 用 V9 stable composite ID ✓
- `UNIQUE(userId,reviewItemId)` ✓
- removedAt tombstone ✓
- 服务端"newer event wins"：实际代码是直接 upsert 覆盖 mastery/dueDate，**没有按事件时间比较** — 只是 last-write-wins（按 updatedAt）。比交付报告声称的"newer review event wins"弱。

---

## 14. Daily Plan

- `UNIQUE(userId,planDate)` ✓
- `mergeDailyPlan` completedTaskIds 并集 ✓（纯函数 + 服务端）
- 客户端 dedupe 风险见 §8

---

## 15. Namespace / Account Switching

| 项 | 结果 |
|---|---|
| `nsKey` guest vs user | 纯函数正确 |
| `migrateLegacyToNamespace` | **从未被调用** |
| Provider 切换账号后 rehydrate | **未实现**（现有 Provider 全是 client-side localStorage 直读，没有 Auth 触发的 reload） |
| A→B 同设备隔离 | namespace 设计上隔离，但 React 状态在换账号后不会自动 reset，B 看到的仍是 A 在内存里的 state — **未测，疑似 P1** |

---

## 16. Source-of-Truth Matrix（实际代码）

| Domain | Guest | Logged-in Working | Durable | 实际冲突策略 |
|---|---|---|---|---|
| Profile | localStorage | localStorage | Cloud（未接） | 代码 LWW，未接 |
| XP | Local Ledger | Local Ledger | XpEvent 表 | Event union（仅服务端） |
| Sessions | localStorage | localStorage | LearningSession 表 | ID merge |
| Wordbook | localStorage | localStorage | WordbookEntry | tombstone |
| Review | localStorage | localStorage | ReviewItem | LWW（非 newer-event-wins） |
| DailyPlan | localStorage | localStorage | DailyPlanState | completed union |
| Settings | localStorage | localStorage | UserSettings | LWW |

**关键差异**：交付报告说"Logged-in Working Source = Local Cache (user:<id>:...)"，**实际代码从未读写 `user:<id>:` 前缀**——所有 Provider 还是直接读 legacy keys。

---

## 17. DB Matrix

| Model | Table | PK | User FK | Unique | Index | Version | Tombstone | Migration Present | Real DB Verified |
|---|---|---|---|---|---|---|---|---|---|
| User | User | cuid | — | email | — | — | — | **NO** | NO |
| Profile | Profile | cuid | userId | userId | — | version | — | **NO** | NO |
| StudyPreferences | StudyPreferences | cuid | userId | userId | — | version | — | **NO** | NO |
| LearningSession | LearningSession | client uuid | userId | (userId,id) | (userId,planDate) | version | — | **NO** | NO |
| XpEvent | XpEvent | cuid | userId | (userId,eventId) | (userId,earnedAt) | — | — | **NO** | NO |
| WordbookEntry | WordbookEntry | cuid | userId | (userId,wordId) | (userId,removedAt) | version | removedAt | **NO** | NO |
| ReviewItem | ReviewItem | cuid | userId | (userId,reviewItemId) | (userId,dueDate) | version | removedAt | **NO** | NO |
| DailyPlanState | DailyPlanState | cuid | userId | (userId,planDate) | — | version | — | **NO** | NO |
| TranslationHistory | TranslationHistory | cuid | userId | (userId,itemId) | — | — | — | **NO** | NO |
| WritingHistory | WritingHistory | cuid | userId | (userId,itemId) | — | — | — | **NO** | NO |
| UserSettings | UserSettings | cuid | userId | userId | — | version | — | **NO** | NO |
| SyncMutation | SyncMutation | cuid | userId | (userId,mutationId) | (userId,status) | — | — | **NO** | NO |
| MigrationRecord | MigrationRecord | cuid | userId | (userId,migrationId) | — | — | — | **NO** | NO |
| QuestionAttempt | — | — | — | — | — | — | — | **表缺失** | NO |

注：QuestionAttempt 表未建；attempt 数据只塞在 `LearningSession.payload` JSON 里，跨设备 accuracy/review 恢复依赖 V9/V10 已有数据。

---

## 18. Sync Matrix

| Domain | Local Source | Remote Source | Push | Pull | Dedupe | Conflict | Delete | Offline | Multi-device | Result |
|---|---|---|---|---|---|---|---|---|---|---|
| Profile | localStorage | Profile | **未接** | ✓ API | n/a | LWW | n/a | n/a | n/a | 未接 |
| XP | Local Ledger | XpEvent | **未接** | ✓ | UNIQUE(eventId) | union | n/a | 队列有/未接 | 未跑 | 骨架 |
| Streak | 派生 | **未同步** | n/a | n/a | n/a | 派生 | n/a | n/a | n/a | **未实现** |
| Sessions | localStorage | LearningSession | **未接** | ✓ | UNIQUE(id) | ID merge | n/a | 队列有/未接 | 未跑 | 骨架 |
| Wordbook | localStorage | WordbookEntry | **未接** | ✓ | UNIQUE(wordId) | tombstone | removedAt | 队列有/未接 | 未跑 | 骨架 |
| Review | localStorage | ReviewItem | **未接** | ✓ | UNIQUE(reviewItemId) | LWW | removedAt | 队列有/未接 | 未跑 | 骨架 |
| DailyPlan | localStorage | DailyPlanState | **未接** | ✓ | UNIQUE(planDate) | completed union | n/a | 队列有/未接 | 未跑 | 骨架 |
| Settings | localStorage | UserSettings | **未接** | ✓ | UNIQUE(userId) | LWW | n/a | 队列有/未接 | 未跑 | 骨架 |
| Translation | localStorage | TranslationHistory | **未接** | **未拉** | UNIQUE(itemId) | ID union | n/a | n/a | 未跑 | 骨架 |
| Writing | localStorage | WritingHistory | **未接** | **未拉** | UNIQUE(itemId) | ID union | n/a | n/a | 未跑 | 骨架 |

---

## 19. E2E Matrix

| 场景 | 实际执行 | 结果 | 证据 | 判定 |
|---|---|---|---|---|
| Register | 未跑（无 DB） | — | — | **NOT VERIFIED** |
| Login | 未跑 | — | — | **NOT VERIFIED** |
| Logout | 未跑 | — | — | **NOT VERIFIED** |
| Guest learning | 未跑浏览器 | — | 单测 263 过 | PARTIAL |
| Guest→Cloud migration | 代码未实现 | — | migrate 函数零调用 | **FAIL（未实现）** |
| Migration repeat | 未跑 | — | — | NOT VERIFIED |
| Device A→B | 未跑 | — | — | NOT VERIFIED |
| Offline→Online | 未跑 | — | — | NOT VERIFIED |
| Dual XP | 纯函数单测 | sum=25 | tests/v12-conflict.test.ts | PARTIAL（仅单测） |
| Wordbook union | 纯函数单测 | 双并集 | 同上 | PARTIAL |
| Wordbook duplicate | 纯函数单测 | 1 条 | 同上 | PARTIAL |
| Wordbook delete | 纯函数单测 | tombstone 胜 | 同上 | PARTIAL |
| Review conflict | 纯函数单测 | newer 胜 | 同上 | PARTIAL |
| DailyPlan completed-wins | 纯函数单测 | 并集 | tests/v12-sync.test.ts | PARTIAL |
| A/B isolation | 未跑 HTTP | — | — | NOT VERIFIED |
| Account switching | 未跑 | — | Provider 未 reset | **SUSPECT FAIL** |

---

## 20. Security

- AUTH_SECRET 未硬编码 ✓
- 无 .env 进 git ✓
- 无 DATABASE_URL 真实值进 git ✓
- 密码 bcrypt cost=12 ✓
- 服务端错误不 stack-trace ✓（register route 只返回"注册失败，请稍后重试"）
- **XP amount 无上限** — P1 任意注入
- **无 rate limit** — P1
- 跨用户隔离未真实 HTTP 测试

---

## 21. Mobile / Desktop / Console

- 未跑 dev server，未跑 375/390/430/1280 视口
- 新页面 /login /register /me/account 用 Tailwind 现有样式，代码层无横向滚动风险
- Console 错误 / hydration warning：未跑

---

## 22. Automated Tests

| 项 | 结果 |
|---|---|
| `npm test` | 311/311 通过 |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过（之前 V12 commit 时） |
| `npx prisma validate` | 通过（fake DATABASE_URL） |
| `npx prisma generate` | 通过 |
| `npx prisma migrate deploy` | **未跑，无 migration** |
| `npx prisma migrate status` | **未跑** |

**测试质量**：V12 新增的 48 条测试全是纯函数（merge/backoff/dedupe/namespace/conflict 逻辑），**没有任何**：
- 真实 Prisma repository test
- Auth API integration test
- Sync API integration test
- Migration integration test
- Cross-user integration test

**P2 测试缺口**：所有"DB 层幂等/隔离"只靠 schema 约束设计，未验证。

---

## 23. V4–V11 Regression

- 263 条旧测试全过 ✓
- Guest 路径代码零改动 ✓
- V10 content:validate 0 errors ✓
- 未跑浏览器真实回归

---

## 24. P0

无（未发现已发生的数据破坏/跨用户泄露/secret 泄露，因为代码根本没接业务，也没跑起来）。

---

## 25. P1（阻塞 V12 验收）

1. **没有版本化 Prisma Migration**：`prisma/migrations/` 不存在，`prisma migrate deploy` 无文件可跑。
2. **没有真实 PostgreSQL E2E 环境**：本机无 Docker / psql / PG 服务，注册→登录→sync→跨用户隔离全部未跑。
3. **Sync Engine 未接入任何业务 Provider**：`enqueueMutation` 零调用，用户做题/收藏/复习不会自动入队；"立即同步"按钮 push 空队列。
4. **Local→Cloud Migration Pipeline 未实现**：migrateLegacyToNamespace 零调用，无 Preview UI，无 Detect/Validate/Normalize/Transaction/Verify。
5. **没有 Server Transaction**：`prisma.$transaction` 零调用，批量 push 中途失败会半成功。
6. **XP amount 无上限/无 source 白名单**：登录用户可自加任意 XP。
7. **登录/注册无任何 rate limit**：撞库/暴力破解零防护。
8. **Review 冲突策略实际是 LWW，不是交付报告声称的 newer-event-wins**。
9. **Account switching 后 React Provider 未 reset**：A→B 同设备，B 可能看到 A 在内存里的 state。

---

## 26. P2

1. Pull 是全量，无 cursor（V12 1.0 可接受，但架构未留接口）。
2. 测试覆盖缺口：无真实 DB / API / 跨用户集成测试。
3. Cookie Secure 未显式配置。
4. `prisma migrate deploy` 并发同 mutationId 是 findUnique-then-create，race 下 500 而不是 409。
5. Streak 完全未同步（schema 无 streak 表，跨设备 streak 会丢）。
6. QuestionAttempt 未独立建表，塞进 session.payload JSON。

---

## 27. P3

1. 文档与实现差距大（交付报告把骨架写成"已完成"）。
2. /me/account 按钮文案与实际行为不符（"合并到账号"实际什么都没合并）。
3. 未做 mobile 视口验收。

---

## 28. 10 个核心问题（按真实情况重答）

| # | 问题 | 答案 |
|---|---|---|
| 1 | Guest 不登录仍可完整学习？ | **YES**（代码层未动 guest 路径，263 旧测试过） |
| 2 | Guest 数据经真实 PG E2E 迁移到 Cloud？ | **NO**（迁移代码未实现） |
| 3 | 重复 migration 在真实 DB 不重复业务数据？ | **PARTIAL**（schema 约束设计在，未真实跑） |
| 4 | 同账号换真实隔离设备恢复核心数据？ | **NO**（pull API 存在，但 Provider 未接，浏览器未跑） |
| 5 | 真实离线→恢复网络正确同步？ | **NO**（队列骨架在，业务未入队） |
| 6 | 双设备同时 XP 真实 DB 验证不丢不重？ | **PARTIAL**（纯函数单测过，未真实 DB） |
| 7 | Wordbook/Review/DailyPlan 冲突策略经真实多设备验证？ | **NO**（纯单测，未真实多设备） |
| 8 | User A 经实际 API 测试无法访问 User B？ | **PARTIAL**（代码层忽略 body userId，未真实 HTTP） |
| 9 | V4–V11 继续正常、旧数据无需清空？ | **YES**（263 旧测试过，guest keys 未动） |
| 10 | 未来加 Phone/WeChat 不用重写业务层？ | **YES**（NextAuth providers 扩展点在） |

---

## 29. 是否允许

- merge main：**不允许**
- tag v12.0：**不允许**
- GitHub Release：**不允许**

---

## 30. 最终建议

V12 当前是**代码骨架 + 单测，不是可验收的云同步产品**。要到"可以验收"，至少需要：

1. 用 `prisma migrate dev --name init` 生成并提交 `prisma/migrations/`。
2. 在 disposable PostgreSQL（或本机起一个临时 PG）上跑通：register → login → 做题 → enqueue → push → pull → 跨用户隔离 → 双设备合并。
3. 把 VocabularyProvider / ReviewProvider / DailyPlanProvider / ProfileProvider 的本地变更接到 `enqueueMutation`，让"做题→入队"真实发生。
4. 实现 Migration Preview UI + 用户确认后再 `migrateLegacyToNamespace` + push + verify。
5. 用 `prisma.$transaction` 包批量 push。
6. XP amount 加白名单（0 ≤ amount ≤ 1000，source 必须是已知值）。
7. register/login 加基础 rate limit abstraction。
8. 账号切换时强制 reset 所有 client Provider（reload + rehydrate）。
9. Review 冲突改成按事件时间/version 比较，不是 LWW。
10. 补真实 DB / API / 跨用户集成测试。

审计完成，未改任何业务代码、未 commit、未 push、未 merge、未 tag。等待人工复核。
