# V7 交付说明：翻译 + 写作专项

分支：`feature/v7-writing-translation`（从 main `6700567` 创建）
日期：2026-09-20
状态：已开发、已测试、已推送 feature 分支，等待人工验收。**未合并 main、未打 v7.0 tag、未建 Release。**

## 一、新增路由

- `/practice/translation` — 翻译首页
- `/practice/translation/session/[id]` — 翻译答题/结果页
- `/practice/translation/complete/[id]` — 翻译完成结算页
- `/practice/writing` — 写作首页
- `/practice/writing/session/[id]` — 写作答题/结果页
- `/practice/writing/complete/[id]` — 写作完成结算页

## 二、新增组件

共享：
- `src/types/subjective.ts` — `SubjectiveFeedback` / `SubjectiveIssue` / `SubjectiveFeedbackDetail` / `SubjectiveSessionMode`
- `src/components/subjective/subjective.css` — 翻译/写作共享样式

翻译：
- `src/types/translation.ts`
- `src/data/mockTranslation.ts`（8 个原创 CET-6 风格中译英任务）
- `src/lib/translation/{scoring,session,xp,storage,store}.ts`
- `src/components/translation/{TranslationProvider,TranslationHome,TranslationSessionPlayer,TranslationComplete}.tsx`

写作：
- `src/types/writing.ts`
- `src/data/mockWriting.ts`（8 个原创 CET-6 风格作文题）
- `src/lib/writing/{scoring,session,xp,storage,store}.ts`
- `src/components/writing/{WritingProvider,WritingHome,WritingSessionPlayer,WritingComplete}.tsx`

修改：
- `src/app/layout.tsx` — 挂载 TranslationProvider、WritingProvider
- `src/components/PracticeModules.tsx` — 首页翻译/写作卡片显示今日 x/1
- `src/components/AppShell.tsx` — exercise 路径白名单加 translation/writing

## 三、TranslationTask 数据结构

```ts
{
  id, title, level: "sentence" | "paragraph",
  promptChinese, keywords[], referenceTranslation,
  scoringPoints[], mockFeedback{summary,issues,details},
  estimatedMinutes, sourceType: "mock"
}
```

## 四、WritingTask 数据结构

```ts
{
  id, title, level: "paragraph" | "essay",
  prompt, requirements[], suggestedWords[],
  referenceEssay, outline[{type,content}],
  scoringPoints[], mockFeedback{summary,issues,details},
  suggestedWordsRange: [min, max], sourceType: "mock"
}
```

## 五、共享 SubjectiveFeedback

```ts
{
  score, maxScore, summary,
  issues: [{type, title, description, severity}],
  details: [{excerpt, userExpression, referenceExpression, note}],
  provider: "mock",   // 未来 "ai"
  createdAt
}
```

## 六、Mock 批改

- 翻译：基础分 3 + 关键词覆盖分（0–9）+ 长度分（0–3），满分 15。
- 写作：基础分 2 + 字数分（0–4）+ 段落分（0–3）+ 关键词分（0–4）+ 连接词分（0–2），满分 15。
- 代码中 `provider: "mock"` 硬编码，未来接 AI 时只需替换 scoring 函数，前端结果页结构不变。

## 七、草稿保存

- 每次输入 dispatch `set_draft`，写入 session.draft，立即 localStorage。
- 刷新/退出/关闭后重进，session 仍在 drafting 阶段，draft 原样恢复。
- 不做"恐吓式退出提示"，只在输入后短暂显示"草稿已保存"。

## 八、历史记录

- `store.history: {taskId, submittedText, feedback, score, createdAt}[]`（写作多一个 `wordCount`）。
- 每次完成自动追加一条；首页底部显示最近 3 次，可删除。

## 九、XP

- 基础 XP：翻译/写作各 12。
- 表现分：按得分占比 0–3。
- 首次完成每篇 = 基础 + 表现分；跨日重复同一篇 = 仅表现分；同日重复同一篇 = 0（防刷分）。
- 账本 key：`translation:first:<taskId>` / `translation:day:<date>:<taskId>`，写作同理。

## 十、每日与额外统计

- 每日各 1 篇，`pickDaily*` 按 epochDay 确定性轮换。
- 完成后"再练一篇"走 extra 模式，`dayStats.extra` 计数，不影响今日 1/1。

## 十一、LocalStorage

- `cet-daily:v1:translation`、`cet-daily:v1:writing`，schemaVersion=1。
- 未改 `cet-daily:v3:vocabulary` / `v1:reading` / `v1:listening`。
- 坏数据隔离：schemaVersion 不匹配重置；单条坏 session/history 跳过并提示。

## 十二、测试

- 新增 `tests/translation.test.ts`（14 项）+ `tests/writing.test.ts`（16 项）。
- 总计 107/107 通过（V6 基线 77 + V7 新增 30）。
- 覆盖：每日生成、草稿保存/恢复、提交、Mock Feedback、参考译文提交前不可见、完成统计、额外练习、XP 去重、历史记录、schemaVersion、坏数据隔离、日期切换、刷新恢复、字数统计。

## 十三、验证

- `npm test`：107/107
- `npm run typecheck`：通过
- `npm run lint`：通过
- `npm run build`：通过（所有 V7 路由已生成）

## 十四、已知限制

- Mock 批改是简单规则，不是真实六级评分。
- 参考译文/范文是原创 mock，不抄真题。
- 历史记录只在各专项内部，未做全局"我的-全部练习"聚合。
- 未接真实 AI API、未做后端/登录/云同步。

## 十五、未来 AI 接入方式

- 保持 `SubjectiveFeedback` 结构不变，把 `scoreTranslation` / `scoreWriting` 替换为调用真实 AI 的异步函数，`provider` 改为 `"ai"`。
- 前端结果页、历史记录、LocalStorage、XP 账本都不需要改。
