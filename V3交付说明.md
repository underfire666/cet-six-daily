# 六级日常 V3 交付说明

项目：`E:\The king of CET`。打开 http://127.0.0.1:3000 ，从首页点击「词汇」。停止服务后可双击「启动网站.cmd」，或运行 `npm run dev`。技术栈延续 Next.js App Router、TypeScript、Tailwind CSS。

## 1. V2 修复

- 答错反馈标题改为「解析」，简短解析优先，详细解析默认折叠。
- 阅读正文、选项和展开反馈可滚动，检查／继续按钮区域与正文分开。
- 实际显示 Mock AI 提示时立即记录 `usedAiHint = true`，提示后答对记为 `ai_hint_correct`，刷新可恢复。
- 完成页「本次未掌握」可点击，展示题目、正确答案、简短及详细解析，不强制再次答题。
- 退出时关闭弹窗，避免返回页面后保留旧弹窗。

## 2. 页面

| 路径 | 内容 |
| --- | --- |
| `/practice/vocabulary` | 今日计划、到期复习、生词本 |
| `/practice/vocabulary/session/[id]` | 新词卡片及巩固，或复习 |
| `/practice/vocabulary/complete/[id]` | 本次词数、独立答对、需要加强、XP |
| `/practice/vocabulary/wordbook` | 搜索、发音、详情、移除及主动复习 |

详情使用弹窗。学习和完成页隐藏首页底部导航。其余四项专项保持占位。

## 3. 核心组件与逻辑分层

`src/components/vocabulary/`：VocabularyProvider、VocabularyDashboard、VocabularyPlayer、VocabularyComplete、WordCard（含 WordAudioButton）、WordbookList。

复用原有 QuestionRenderer、LessonProgress、AnswerFeedback、AiHint、退出弹窗和答题状态机。新增 `UnmasteredReview` 同时供每日关卡与词汇使用。

`src/lib/vocabulary/` 分离 mastery、reviewScheduler、wordAudio、xp、questions、session、store、storage。数据在 `src/data/mockVocabulary.ts`、`mockVocabularyExercises.ts`；类型在 `src/types/vocabulary.ts`。颜色沿用现有 Design Tokens。

## 4. Word 数据结构

`id`、`word`、`phonetic`、`partOfSpeech`、`meaning`、`secondaryMeanings`、`example`、`exampleTranslation`、`difficulty`（1–5）、可选 `audio`、`tags`、`source`、`createdAt`。

`source` 支持 vocabulary / reading / listening。发音服务优先使用配置音频，否则调用浏览器英文语音，优先美式；失败显示提示。

## 5. UserVocabularyState 数据结构

`wordId`、`masteryStatus`、`selfReportedUnknown`、`needsReview`、`firstSeenAt`、`lastReviewedAt`、`nextReviewAt`、`reviewCount`、`correctCount`、`wrongCount`、`consecutiveCorrect`、`lastIndependentAt`、`independentTypes`、`addedToWordbook`、`source`。

时间保存为 ISO 字符串；每日计划与奖励按上海日期区分。收藏与需要复习独立，移出生词本不会删除学习记录或取消到期复习。

## 6. 掌握状态

- `new`：尚未学习。
- `learning`：开始学习，首次答对也停留在此阶段。
- `weak`：选择不认识、答错或依靠提示等，需要加强。
- `reviewing`：距离上次作答至少 24 小时，首次尝试独立答对后进入。
- `mastered`：累计至少 3 次符合间隔要求的连续独立正确，且覆盖英译中、中译英、例句填空三种题型。

立即重复作答不提升掌握等级。答错或非独立正确会清空连续独立正确记录；复测答对不抹去首轮错误。正确／错误次数统计实际提交次数。新词每组 5 个，先看卡片，再巩固，避免不认识后立刻连续出现同一个词。

## 7. Review Scheduler

| 状态 | 下次复习间隔 |
| --- | --- |
| weak | 4 小时 |
| learning | 1 天 |
| reviewing | 随连续独立正确增加为 3、7、14 天 |
| mastered | 30 天 |

`getWordsDueForReview(states, now)` 返回需要复习且已到期的词，按到期时间排序。每组最多 5 个；无到期词显示空状态。生词本主动复习不受时间限制。到期练习完成后重新排期；提前额外答对不会推迟尚未到期的计划。掌握后仍有长间隔复习。

## 8. 本地保存

`cet-daily:v3:vocabulary` 保存版本、每词掌握状态与收藏、每日计划和完成词、会话卡片位置、冻结题目、选择和重试／复测记录、提示状态、完成奖励、XP 去重记录。

保留 V2 profile / session / settings。会话题目冻结，避免其他练习改变掌握状态后导致恢复时题目变化。载入检查版本和结构，隔离损坏记录；本地存储不可用时降级为内存并提示，此时刷新不能保留进度。

## 9. XP

每词首次完成 +2 XP；以后其他学习日复习 +1 XP；同一词同一上海日期再次练习 +0 XP。首次完成一组 5 词通常 +10 XP。奖励按完成计算，不要求全部正确。会话完成与全局 XP 均去重，刷新完成页不重复奖励。词汇 XP 与每日关卡奖励分别记账。

## 10. Mock 范围

30 个示例词，包含音标、释义、原创例句和译文；三类题使用预设干扰项。每日新词默认 20、每组 5，在配置中管理。词库用完显示完成状态，不重复伪造新词。

每日关卡题目、AI 提示、初始用户、历史计划和考试目标仍为 Mock；学习、收藏、复习及 XP 记录由实际本地操作产生。没有接入数据库、登录或真实 AI。

## 11. 验证与已知限制

- 自动测试覆盖日期、V2 答题规则、词汇掌握、到期边界、时区等价时间、复测、持久化、奖励去重及词库耗尽。
- Chrome 检查 375×667、390×844、430×932、1440×900：单词卡、详情、底部按钮无横向溢出或遮挡；长阅读末行、最后选项及展开解析可访问。
- 完成新词学习→错题复测→完成→搜索生词→详情→主动复习，以及原每日关卡完整回归；检查刷新、退出恢复、到期入口、无效会话与存储降级。检查过程未发现应用运行错误。
- 发音失败路径已验证；实际语音质量依赖浏览器和系统语音资源，未人工验证各设备的听感。真实手机硬件、Safari、Firefox 尚未实测。
- 数据仅保存在当前浏览器和站点地址；不跨设备同步、不合并多标签页同时作答。30 个词是验证用有限词库。掌握规则为可替换的简单规则，非经验证的记忆预测模型。

## 12. 完整体验步骤

1. 首页点击「词汇」→「开始学习」。
2. 在第一张单词卡点击发音和「加入生词本」，选择「不认识」；继续看完一组 5 张卡。
3. 完成三类巩固题。可故意首次选错检查「再试一次」，再次选错检查解析，继续到末尾复测。
4. 查看完成页词数、XP；复测仍错时点击「本次未掌握」。刷新确认奖励没有增加。
5. 返回词汇→「查看生词」，输入第一词的英文片段（例如 sus），点击详情并主动复习。
6. 刷新页面，确认收藏、进度和复习记录保留；移出生词本后，掌握与复习记录仍保留。
7. 到达详情显示的复习时间后，从「待复习」开始。新用户没有到期词时显示空状态；也可随时从生词本主动复习。
8. 返回首页，完成一次「今日学习」，检查重试、Mock AI 提示、错题复测和完成页仍正常。

开发检查：`npm test`、`npm run typecheck`、`npm run lint`、`npm run build`。本版完成后等待 UI 检查。
