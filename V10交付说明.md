# V10 交付说明 — CET-6 Content System 1.0

> 版本：V10（`feature/v10-content-system`）｜日期：2026-09-22｜状态：**修复完成，待人工验收**
>
> 重要：**当前内容仍然主要为 Mock**（`src-mock-original`，license=unknown）。
> 本项目不把 Mock 描述成真实 CET-6 题库，不接 AI / 后端 / 登录 / 真题抓取。

## 1. V10 是什么

V10 把"页面直接读 Mock 数组"升级为**统一 Content Layer（内容层）**：

```
页面（词汇/阅读/听力/翻译/写作）→ Content Selector → Content Repository
      → Content Registry → Content Pack（含 Mock/正式内容）
```

正式内容包与 Mock 内容包结构一致。给项目一份合法、结构正确的 ContentPack JSON，
**不需要修改五大学习页面的核心代码**即可导入（见 `src/content/importer.ts`）。

## 2. 组件清单

| 组件 | 文件 | 职责 |
| --- | --- | --- |
| ContentSource | `src/content/types.ts` + `src/content/sources.ts` | 内容来源登记（type / licenseType）；Mock 为 `src-mock-original` |
| ContentPack | `src/content/types.ts` | 内容包：id / contentType / sourceId / items / 版本 / 时间戳 |
| ContentRegistry | `src/content/registry.ts` | 包注册、去重、坏记录隔离；`registerContentPack` / `getContentPack` / `getItems` |
| ContentRepository | `src/content/repositories.ts` | 按类型查询：all / getById / getByTag / getByDifficulty；别名解析 |
| ContentSelector | `src/content/selector.ts` | 确定性选择（同 seed 同池同结果），exclude + 回填 |
| ContentValidator | `src/content/validator.ts` | 结构/字段/答案校验；`validatePack` / `validateAll` |
| ContentImporter | `src/content/importer.ts` | Raw JSON → Parse → Normalize → Validate → Duplicate Check → Pack → Registry |
| Stable ID | `src/content/types.ts` / `src/content/aliases.ts` | 稳定 ID 一次生成不变；别名旧 ID → 新 ID |
| ContentAlias | `src/content/aliases.ts` | 迁移预留（当前 identity） |
| Difficulty Adapter | `src/content/difficulty.ts` | 模块难度（词汇 1–5）→ 统一枚举 easy/normal/hard |
| Review Repository Replay | `src/lib/review/replay.ts` | 错题复习页经 Repository 回放原题，不复制整题 |
| ExamPaper Schema | `src/content/papers.ts` | 试卷结构校验（`validatePaper`） |

## 3. 统一 Difficulty

- 统一枚举：`easy | normal | hard`（与阅读/听力既有难度对齐）。
- 词汇模块内部 `difficulty`（1–5）**保留不改**（V4 词汇逻辑依赖），
  统一难度由 `unifiedDifficulty()` Adapter 映射：1–2→easy、3→normal、4–5→hard。
- 阅读/听力：内容自带 `easy/normal/hard`，原样通过。
- 翻译/写作：内容数据无难度字段，注册时补 `difficulty: "normal"`（不伪造内容）。
- `npm run content:stats` 按统一难度统计，**不再出现无意义的 `unspecified`**。

## 4. Review Repository Replay（V9 错题本增强）

- `ReviewItem` 只保存稳定 ID（sourceModule + sourceActivityId + questionId），
  另存一个最小快照 `lastWrongOptionId?`（用户上次答错的选项 id，兼容旧数据，可缺省）。
- 复习页 `/review/session/[id]` 通过 `replayReviewItem(item)` 从 Content Repository
  找回原题，展示：题干、选项（正确答案/用户作答标色）、简短解析；
  阅读回放关联原文（折叠）、听力回放关联材料与原文（折叠）。
- 旧 ReviewItem / 内容已 deprecated 时**优雅降级**：提示"原题内容已不可用"，仍可自评，不白屏。
- 词汇错题由 V4 自己管理，不重复收录；词汇生词回放使用动态生成的 4 选项题。

## 5. Content Importer 用法

代码层入口（无 CSV/TXT UI、无后台上传页、无数据库/远程 API）：

```ts
import { importContentPackFromJson, importAndRegisterContentPack } from "@/content/importer";

// 只解析+校验（不入库）：
const pack = importContentPackFromJson(rawJson);

// 解析+校验+注册：
importAndRegisterContentPack(rawJson, { strict: true });
```

拒绝（抛 `ContentImportError`，附 issues 明细）：

- invalid JSON
- missing id（pack 或 item）
- unknown source
- duplicate id（包内或跨包注册时）
- invalid correctAnswer（answerId 不在选项内）
- invalid contentType

纯元数据（createdAt/updatedAt/version/status/authenticity/sourceId/type/tags/secondaryMeanings）
缺失时补合理默认；关键字段一律严格校验。

## 6. Schema 补齐

- `skillTags`：ContentMeta 已支持（可选），pack 条目可空。
- `ReadingQuestion.evidence` / `ListeningQuestion.evidence`（可选）：
  `{ paragraphId?: string; sentenceId?: string }`，不强迫 Mock 内容伪造，没有时 undefined。

## 7. 分层约定（V8 Daily Plan）

- V8 PlanGenerator → 决定今天学哪些模块/多少任务。
- V10 ContentSelector → 专项真正启动时决定具体内容。
- 五个专项的 pick/plan 均以 Content Layer 为唯一内容来源（集成测试覆盖），分层不变。

## 8. 验证

- `npm test`：263 项全部通过（含 V10 新增：content、importer、difficulty、replay、集成）。
- `npm run typecheck` / `npm run lint`（0 error 0 warning）/ `npm run build` 全部通过。
- `npm run content:validate`：0 error。
- `npm run content:stats`：vocabulary 30 / reading 6 / listening 7 / translation 8 / writing 8（59 项，Mock）。
- 浏览器验收：错题本、复习答题页（回放原题）、结果页、手机宽度（375/390/430px）无横向溢出。

## 9. 明确不做（本版边界）

- 不开发真实 AI、登录、数据库、后端或真实题库；Mock 不冒充真题。
- 不把翻译、写作强行加入传统客观错题本。
- 不破坏 V4–V9 功能（词汇掌握/生词本、阅读、听力即时变速与草稿恢复、每日计划、历史关卡）。
