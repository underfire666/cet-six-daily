# V12 账号 + 后端 + 云同步 1.0 交付说明

状态：**V12 待验收**（feature/v12-cloud-sync，未 merge main、未打 v12.0 tag、未建 Release）。

## Architecture

```
UI (V4–V11 保持不变)
  ↓
Existing Providers / Domain Services
  ↓
Local Repository (V1–V11 LocalStorage, guest namespace)
  ↓
Sync Engine (client queue + push/pull + retry/backoff)
  ↓
Remote Repository (Next.js Route Handlers under /api/sync/*)
  ↓
Server Auth (NextAuth v5 Credentials + Prisma)
  ↓
PostgreSQL (Prisma ORM)
```

页面不直接知道 SQL / DATABASE_URL；所有 userId 从 server session 取，客户端 body 里的 userId 被忽略。

## Auth

- 方案：NextAuth v5 (beta) + Credentials provider（邮箱 + 密码）。
- 密码：bcryptjs cost=12（V12 第一版；后续可换 Argon2id）。
- Session：JWT（HttpOnly cookie），maxAge 30 天。
- 注册：`POST /api/auth/register`，服务端校验邮箱格式 / 密码长度 / 两次一致；重复邮箱返回 409。
- 登录失败统一返回"邮箱或密码不正确"，不泄露邮箱是否已注册。
- 未来扩展手机号 / 微信：Auth providers 数组追加即可，业务层不感知。

## Database

- PostgreSQL，Prisma 6 作为 ORM，schema 在 `prisma/schema.prisma`。
- 实体：User / Profile / StudyPreferences / LearningSession / XpEvent / WordbookEntry / ReviewItem / DailyPlanState / TranslationHistory / WritingHistory / UserSettings / SyncMutation / MigrationRecord。
- 关键唯一约束：
  - `User.email` unique
  - `XpEvent (userId, eventId)` unique
  - `WordbookEntry (userId, wordId)` unique
  - `ReviewItem (userId, reviewItemId)` unique
  - `DailyPlanState (userId, planDate)` unique
  - `SyncMutation (userId, mutationId)` unique（幂等）
  - `MigrationRecord (userId, migrationId)` unique（迁移幂等）
- Migration：版本化 SQL（用户首次部署时执行 `prisma migrate deploy`；本次交付未在本机跑 migrate，因为本地没有 DATABASE_URL）。

## Local-first / Offline

- 用户操作先更新 Local State 与 LocalStorage，再入 SyncMutation 队列（`cet-daily:v12:sync-queue`）。
- UI 立即反馈；网络失败标记 `pending/failed`，恢复网络自动 retry，指数退避（cap 5min）。
- Guest 模式完全不变：不登录也能学习，数据留在本地。

## Sync Engine

- Client：`src/lib/sync/client.ts`（enqueue / push / pull / dedupe / backoff）。
- Server：`src/lib/sync/server.ts`（按 entityType 路由，事务安全 upsert）。
- Idempotency：每条 mutation 带 `mutationId`，服务端写 `SyncMutation` 表；重复推送命中 unique 约束即 skip。

## Conflict Matrix（V12 第一版）

| Entity | ID | Create | Update | Delete | Conflict | Dedupe | Version | Tombstone |
|---|---|---|---|---|---|---|---|---|
| XpEvent | client eventId | insertOrIgnore | n/a | n/a | set union | UNIQUE(userId,eventId) | n/a | n/a |
| LearningSession | client UUID | upsert by id | upsert | n/a | ID merge | UNIQUE(userId,id) | version++ | n/a |
| Wordbook | userId+wordId | upsert | upsert | removedAt=now | tombstone wins | UNIQUE(userId,wordId) | version++ | removedAt |
| ReviewItem | stable composite | upsert | upsert | removedAt=now | newer event wins | UNIQUE(userId,reviewItemId) | version++ | removedAt |
| DailyPlan | userId+planDate | upsert | merge completed | n/a | completed-wins monotonic | UNIQUE(userId,planDate) | version++ | n/a |
| Profile | userId | upsert | LWW | n/a | LWW | unique | version++ | n/a |
| Preferences/Settings | userId | upsert | LWW | n/a | LWW | unique | version++ | n/a |
| Translation/Writing | client itemId | insertOrIgnore | n/a | n/a | ID union | UNIQUE(userId,itemId) | n/a | n/a |

## Local → Cloud Migration

- Pipeline：Detect（local keys）→ Preview（学习天数/XP/词数/错题数）→ Merge（按 conflict matrix）→ Upload（push 队列）→ Verify（pull 回来比对）→ MarkMigrated（MigrationRecord）。
- migrationId 是 client 生成的 UUID，服务端 UNIQUE(userId,migrationId)，重复提交不重复副作用。
- 迁移成功后本地数据继续作 cache；失败不清空本地。

## User namespace

- Guest：继续用 legacy keys（`cet-daily:*`），零迁移零破坏。
- 登录用户：读写 `user:<id>:cet-daily:*`；`migrateLegacyToNamespace()` 首次登录时把 guest 的 legacy 值复制一份到 user namespace，**不删除** guest 原始数据。
- 登出：保留 user namespace cache（按 V12 §79 谨慎策略）；换账号时 B 看不到 A 数据，因为 key 前缀不同。

## Security

- 所有 `/api/sync/*` 从 `auth()` 取 userId，忽略 body 中的 userId。
- 服务端校验 request body（validateMutation），未知 entity / 缺字段直接 400。
- 错误信息统一中文友好，不泄露 SQL / stack / DATABASE_URL。
- `.env` / `.env.local` 在 .gitignore；`.env.example` 只放占位。

## Time

- DB timestamp 一律 UTC（`@db.Timestamptz`）。
- 业务日期（planDate / dueDate / study day）继续 Asia/Shanghai，保持 V8/V9 行为。

## Testing

- 纯逻辑单测（`tests/v12-sync.test.ts`、`tests/v12-conflict.test.ts`）：
  - mutation 校验
  - DailyPlan completed-wins merge
  - backoff 上限
  - 队列同实体去重
  - Wordbook tombstone 胜出 / 同词合一 / 双设备并集
  - XP event 去重求和
  - Review newer-event-wins
  - namespace key 命名
- 未在本环境跑真实 DB 集成测试（无 PostgreSQL 实例）；Prisma schema 已就位，部署后 `prisma migrate deploy && prisma generate` 即可建表。

## Known Limitations

- 本机未启动 PostgreSQL，未执行 `prisma migrate`；`npm run build` 在缺 DATABASE_URL 时仅做类型检查，不连库。
- 未做 rate limiting（V12 第一版可接受）。
- 未做邮件验证 / 密码重置（V13+）。
- 手机号 / 微信登录接口已预留 provider 扩展点，未实现。

## V13 boundary

不做：AI 批改、微信/短信登录、会员、支付、排行榜、App 壳、推送。
