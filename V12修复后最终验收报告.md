# V12 修复后最终验收报告

分支：`feature/v12-cloud-sync`
Commit：`1eced92`（基于初版 `c126a48` 之上）
日期：2026-09-23

## 一、修复范围（对应审计 P1）

| # | 审计项 | 修复 | 验证 |
|---|---|---|---|
| 1 | 无 prisma migration | 手写 PostgreSQL 初始 migration（13 表、FK、unique index、cascade）| 文件存在，DDL 与 schema.prisma 一致 |
| 2 | 服务端无 $transaction | 每个 mutation 包在 `prisma.$transaction` 内，同时写 SyncMutation 幂等记录 | typecheck 通过 |
| 3 | XP 无白名单/上限 | source 白名单（9 个）+ amount 1–200 范围校验 | 新增 3 条单测覆盖 |
| 4 | 无 rate limit | 内存版 10 次/60s，register 按 IP，login 按 IP+email | 代码接入 register route + NextAuth authorize |
| 5 | Review 实际是 LWW | version-aware：`incoming.version < existing.version` 丢弃 | 服务端 upsert 前 compare |
| 6 | 并发重复 push | catch Prisma P2002 unique conflict → skipped（幂等）| server.ts applyPushBatch |
| 7 | 客户端一刀切 dedupe | per-entity merge：snapshot / event / accumulative / state | client.ts MERGE_POLICY |
| 8 | 无 pull hydrate | `/api/sync/pull` 响应写回 user-scoped localStorage | hydrate.ts |
| 9 | 无 online 监听 | SyncProvider 监听 window.online + 60s 轮询 | SyncProvider.tsx |
| 10 | Migration Preview UI | 账号页检测本机数据（学习天数/XP/生词/错题）→ [合并到我的账号] / [稍后处理] | account/page.tsx |
| 11 | AppProviders 未接入 layout | layout 用 SessionProvider + SyncProvider 包裹整树 | layout.tsx |
| 12 | DailyPlan 合并 | 服务端 mergeDailyPlan：completedTaskIds 并集（monotonic）| server.ts |

## 二、验证结果

| 命令 | 结果 |
|---|---|
| `npm run typecheck` | 0 error |
| `npm run lint` | 0 error，1 warning（`applyMutation` 未使用的旧代码保留作参考）|
| `npm test` | **314/314 通过**（V1–V11 263 + V12 纯函数 51）|
| `npm run build` | 成功，全部路由正常生成 |

## 三、未完成 / 已知问题

1. **POSTGRES E2E NOT VERIFIED** — 本机无 Docker、无 psql、无 PostgreSQL 服务。以下流程未在真实 DB 上跑过：
   - 注册 → 登录 → push → pull → hydrate
   - 跨用户数据隔离
   - 双设备并发 push 合并
   - migration SQL 在真 PG 上执行
   - 生产环境 AuthSecret / DATABASE_URL 未配置
2. **业务层 sync adapters 未接入 Provider** — 现有 VocabularyProvider / ReviewProvider / DailyPlanProvider 的本地 mutation 还没调用 `enqueueMutation()`。当前 SyncEngine 只在 Migration Preview 手动触发时 push；日常做题/收藏/复习不会自动入队。这是 V12 骨架到"真同步"的最后一公里。
3. **账号切换 Provider 未 reset** — A→B 同设备登录后，React 内存 state 仍是 A 的数据（需 key={userId} 重挂载或 useEffect 依赖 userId 重 hydrate）。
4. **StorageContext 未真正接入** — 现有 Provider 仍直接读 `cet-daily:*` legacy key，没按 user scope 切换。hydrate 只写 user namespace，不影响 guest 旧数据。
5. **Review version 字段** 已在 schema，但业务层写 ReviewItem 时还没递增 version；version-aware 冲突当前只对"未来接入的 adapters"生效。

## 四、Git 状态

- 当前分支：`feature/v12-cloud-sync`
- 本地 HEAD：`1eced92`
- 远程已 push：`c126a48..1eced92`
- **未** merge main
- **未** 打 v12.0 tag
- **未** 建 Release

## 五、下一步建议（不在本次范围）

- 配真实 PostgreSQL（Supabase / Neon / 自建），跑 `prisma migrate deploy`，做 E2E
- 把业务 Provider 的关键 mutation 点接上 `enqueueMutation()`
- 账号切换时 `key={userId}` 重挂 SyncProvider + 所有业务 Provider
- 在 V13 把 StorageContext 做成统一 hook
