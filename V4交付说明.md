# V4 交付说明

## 已实现

- 每日词汇保持 20 词目标，完成后显示 20/20 ✓，额外学习不改变每日总关卡和 Streak。
- 完成区域提供「继续学习」，可选再学 10、20、自定义正整数。没有每日累计学习数量上限；按需创建每组最多 5 词的会话。
- 完整复用卡片、发音、三类题型、重试、解析、Mock AI 提示、错题复测、收藏、掌握记录、复习计划及 XP。
- 每小组完成后保存成果，批次完成页显示本批次总量、独立正确、需要加强和奖励，可再次选择数量。
- 首页单独显示今日额外学习数量和今日词汇 XP。额外学习属于练习词次数，包含旧词巩固。
- 未完成批次可退出、刷新及跨日恢复；次日仍可独立开始自己的 20 词任务。

## 修改文件与新增组件

相对项目根目录 `E:\The king of CET`：

| 文件 | 本版改动 |
| --- | --- |
| `src/components/vocabulary/ExtraLearningDialog.tsx` | 新增数量选择弹窗，复用 LessonDialog |
| `src/components/vocabulary/VocabularyDashboard.tsx` | 20/20 完成状态、额外入口、单独统计及跨日续学入口 |
| `src/components/vocabulary/VocabularyComplete.tsx` | 小组／批次完成、批次总计、继续选择数量 |
| `src/components/vocabulary/VocabularyPlayer.tsx` | 额外学习卡片流程和批次进度 |
| `src/components/vocabulary/VocabularyProvider.tsx` | 连接批次、统计与保存，区分当天和旧日会话 |
| `src/components/vocabulary/WordCard.tsx` | 卡片标题适用于新词和旧词巩固 |
| `src/components/vocabulary/vocabulary.css` | 数量弹窗、输入和紧凑统计样式 |
| `src/types/vocabulary.ts` | extra 模式、批次类型及完成时间 |
| `src/lib/vocabulary/store.ts` | 每日固定任务量、额外组生成、批次及当日汇总 |
| `src/lib/vocabulary/session.ts` | 额外学习复用卡片和答题状态机 |
| `src/lib/vocabulary/storage.ts` | 版本 1 兼容读取、版本 2 保存和批次校验 |
| `src/lib/vocabulary/mastery.ts` | 重看卡片点击认识不推迟已安排的复习时间 |
| `tests/vocabulary.test.ts` | V4 额外学习、计数、迁移和跨日测试 |
| `PROJECT_CONTEXT.md` | 新建项目上下文，记录当前 V4 与后续边界 |
| `README.md` | 更新当前版本与文档入口 |

本版沿用原路由，没有增加独立的第二套词汇答题页面。

## 新增数据与本地保存

- `VocabularySessionMode` 新增 `extra`。
- `VocabularySession` 新增可选 `batchId`、`completedAt`。
- `ExtraVocabularyBatch` 包含 `id`、`target`、`createdAt`、`sessionIds`。
- `VocabularyStore` 新增 `batches`，保存版本升级为 2，继续使用原 localStorage 键 `cet-daily:v3:vocabulary`，避免丢失原记录。
- 保存每日任务和完成词、词汇掌握状态、收藏、当前卡片和答题进度、额外批次、完成时间与 XP 去重账本。额外数量和完成状态从这些已保存数据推导，不维护容易失步的重复计数。
- 按上海日期归属：每日任务记在原计划日；额外数量和 XP 记在小组实际完成日。
- V3 的有效会话、收藏和奖励兼容恢复；无效记录隔离，存储不可用时显示提示并退回内存。

## 示例词库与奖励

仍使用 30 个示例词。未学词优先，新词不足时轮换旧词巩固；每天安排 20 个任务词，额外学习不扣减第二天的名额。页面明确提示可能包含已学词。组内没有重复词，旧词不会被重置成未学状态。

每词首次完成 +2 XP，以后不同日复习 +1，同词同日重复 +0。首次学完全部 30 个词可获得 60 词汇 XP，当日继续练习仍能增加额外学习量，但不会继续刷取同词奖励。原每日总关卡奖励单独保留。

## 验证

- 29 项自动测试通过，包括 V2 规则、V3 掌握逻辑，以及 V4 每日 20→额外 10→额外 20、30 次额外统计、次日 20 词、跨日奖励、无效／大数量、7 词分组、旧数据迁移与坏批次隔离。
- Chrome 实际完成每日 20、额外 10、额外 20，以及自定义 7 和 2 词；确认 20/20 不变、收藏共用、刷新恢复、退出续学和奖励去重。
- 检查额外学习的答错重试、解析、提示后正确、复测及未掌握详情。
- 检查 375、390、430、1440px：词汇首页、数量弹窗和卡片无横向溢出、正文不被底栏遮挡。
- 原每日总关卡、长阅读、AI 提示、复测、完成奖励和四个占位专项回归通过。浏览器检查未发现应用运行错误。
- 检查命令：`npm test`、`npm run typecheck`、`npm run lint`、`npm run build`。

## 当前限制与体验方法

没有发现阻断上述流程的问题。示例词池有限，额外数量不表示不同新词数量。单组未完成时已保存答题进度，但该组尚不计入已完成额外数量。实际发音依赖系统语音资源；真实手机硬件、Safari、Firefox 未实测。本地记录不会跨设备同步，不处理多标签页同时写入的冲突。

本机开发服务日志出现一次 Watchpack 扫描 `E:\System Volume Information` 的 EINVAL 提示；页面请求正常返回 200，生产构建与浏览器流程均通过。本版未改动与词汇功能无关的文件监听配置。

打开 http://127.0.0.1:3000/practice/vocabulary ，完成今日 4 组词汇后点击「继续学习」，选择 10，再选择 20。首页应保持 20/20，额外累计 30。可在卡片收藏、退出重进或刷新检查恢复。

停止服务后可双击项目里的 `启动网站.cmd` 或运行 `npm run dev`。完成 V4 后停止，等待 UI 检查；不自行开始 V5。
