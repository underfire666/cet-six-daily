# V11 交付说明（feature/v11-profile-center · 修复后待人工验收）

## 版本范围

V11 = 「我的」+「设置」+「学习数据中心」1.0。本轮（v11repair 指令）为 V11 独立审计发现的问题做修复，**不开始 V12**；未 merge main、未打 v11.0、未建 Release、未删分支。

当前 HEAD（提交前）：`41cec0e feat: complete V11 profile and study center`，修复改动随本说明一同提交。

## 本轮修复内容（对照审计 8 个核心问题）

1. **移除生产环境 mock 学习基线**
   - `src/lib/lesson/profile.ts` 重写：删除 `mockUser` 引用；`streakFor` 只按 `rewardsByDay` 真实完成日计算（不再把 anchorDate 前 7 天预置为已学）；新增 `NEXT_LEVEL_XP=1500`、`LEVEL_TITLES`（6 档）、`levelFor(totalXp)`（level=1+floor(total/1500)）。
   - `src/data/mock.ts` 删除 `mockUser` 导出；`userFor` 基线为 0，只累计真实奖励 XP 与 bonus XP。
   - 结果：新用户显示 XP 0 / Streak 0 / Lv.1「新手」，等级完全由真实 XP 推导。

2. **Sound / Haptic / Celebration 收敛为单一 Source of Truth**
   - 旧实现：设置页写 `cet-daily:v1:profile.sound`，学习组件消费 `cet-daily:v2:settings`（FeedbackSettings），双套独立、设置不生效。
   - 新实现：`src/lib/profile/store.ts` 删除 `SoundPrefs` 与 `sound` 字段（旧数据中残留 `sound` 字段被 `loadProfile` 忽略，不迁移、不回写）；`/me/settings/sound` 页改写 `FeedbackSettings`（soundEnabled / hapticsEnabled / celebrationEnabled）；`ProfileProvider` 透传 `learning.settings / setSettings`。
   - 学习组件（LessonPlayer / ReadingSessionPlayer / ListeningSessionPlayer / VocabularyPlayer 的 `playFeedback`、完成页庆祝动画）原本就消费该对象，设置现在立即生效。

3. **正确率：修复重复计数与覆盖**
   - `summarizeObjective` 按 `initial[]` + `retest[]` 数组逐次计数；首轮双错（`initialResult="wrong"`）不再额外 +1（此前误计 3 次）；数组缺失时仅用 `initialResult` 兜底 1 次。
   - 覆盖范围：词汇 + 阅读 + 听力会话的 `lesson.records` + 复习 `ReviewSession.answers`（新增 `summarizeReviewAnswers`）；翻译/写作为主观题，不计入客观正确率（页面注明）。
   - `ObjectiveRecord` 类型扩展真实取值：first_try_correct / second_try_correct / ai_hint_correct / wrong / unmastered。

4. **真实学习时长统计**
   - 新增 `summarizeDurations` / `CompletedSession` / `totalDurationSeconds` / `weekly.durationSeconds`。
   - 时长来源全部为真实会话：V2 daily（phase=complete）+ 词汇 + 阅读 + 听力 + 翻译 + 写作 + 复习的 `startedAt → completedAt`；非法/缺失/NaN/负数/超 24 小时记录一律忽略。
   - 历史无时间戳的旧会话不估算、不伪造。Daily Plan `estimatedMinutes` 仅用于计划展示，不当作真实时长。
   - 完成日归属按 Asia/Shanghai 计算（`todayInShanghai`）。

5. **weekly.completedTasks 移除**
   - 原 `completedTasks` 实为「有完成记录的天数」且无完成时间戳可聚合；按修复指令选项 B 删除该字段，UI 改为「本周学习天数 / 本周学习时长 / 本周 XP」三栏（`/me/stats`）。

6. **过去考试日期倒计时**
   - `/me` 使用 `countdownText(today, examDate)`：未来「距离六级考试还有 N 天」、当日「今天是六级考试日」、已过「本次目标日期已结束，请更新考试日期」。

7. **V11 自动测试补齐**
   - 新增 `tests/profile-stats.test.ts`（正确率计数/时长聚合/周统计/格式化）、`tests/profile-store.test.ts`（加载/损坏/旧字段忽略/保存失败/回写）、`tests/profile-integration.test.ts`（零基线、streak、levelFor、stats 集成、settings 单源）。
   - 更新 `tests/lesson.test.ts` 两处断言以匹配零基线语义（45 XP / streak 1 / level 1）。
   - 总计 **294/294** 通过。

8. **lint 0 警告**
   - 清理 `me/page`（Flame/BarChart3）、`me/stats`（Link）、`stats.ts`（todayInShanghai）等未用导入；现 `eslint .` 0 error 0 warning。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| npm test | 294 pass / 0 fail |
| npm run typecheck | 通过（0 错误） |
| npm run lint | 0 error / 0 warning |
| npm run build | 通过 |
| npm run content:validate | Errors 0 / Warnings 0 |
| npm run content:stats | 59 项 mock/unknown，easy 20 / normal 39，Deprecated 0 |

## 浏览器实测结论（v11repair 复测，最终 HEAD 73658db）

- `/me` 新用户空态实测：目标 500 分 · 距离六级考试还有 80 天；Lv.1 · 新手；🔥 0；0 学习天数 / 0 总 XP；正确率「暂无」；本周天数 0。无 mock 基线（1240 XP / Lv.12 / 🔥7 已不存在）。
- 首页 TopStatus：XP 0 · Lv.1 · 新手 · 0/1500；倒计时 80 天；无昵称空白渲染。
- `/me/stats`：总学习天数 / 学习时长（分钟）/ 总 XP / 整体正确率 / 客观题作答 / 本周（学习天数/时长/XP）/ 最近 7 天，全部按真实数据渲染；页脚注明正确率口径（词汇/阅读/听力/复习，翻译写作排除）与时长来源（真实会话，旧记录不估算）。
- 声音与震动页（viewport 399×639）：三个开关（答题音效/震动反馈/完成庆祝动画）均写入同一 `cet-daily:v2:settings`（FeedbackSettings）——关音效 → `{"soundEnabled":false,…}`、关震动 → `{"hapticsEnabled":false,…}`、关庆祝 → `{"celebrationEnabled":false,…}`，恢复后全 true；与学习组件 playFeedback / 完成页庆祝消费的是同一对象。
- 375/390/430px 因工具无法调整 viewport 标 NOT VERIFIED，以 399px 实测 + 移动优先单列布局作辅助证据；桌面宽屏为自然居中布局。

## 边界与约束

- 保留绿色主题、Design Tokens、移动端布局；不开发真实 AI/登录/数据库/后端/真实题库。
- 不把翻译/写作强行纳入客观错题本；不破坏 V4–V8 功能（听力即时变速、草稿恢复、每日计划、历史关卡）。
- 存储失败明确提示（saveProfile 返回 false，不宣称已保存）；坏记录单独隔离。
- 本次未 merge main、未打 tag、未建 Release（等人工验收）。

## 运行

- `npm run dev` → http://127.0.0.1:3000
- 检查命令：`npm test`、`npm run typecheck`、`npm run lint`、`npm run build`、`npm run content:validate`、`npm run content:stats`
