# V12 最终候选验收报告

分支：`feature/v12-cloud-sync`
本地 HEAD：`(刚 commit，待 push——本机到 github.com:443 连接被重置)`
日期：2026-09-23

## 一、诚实分级

| 项目 | 状态 |
|---|---|
| 代码实现 | IMPLEMENTED |
| 纯函数单测 | UNIT TESTED（314/314） |
| 类型检查 | UNIT TESTED（0 error） |
| Lint | UNIT TESTED（0 error 0 warning） |
| Build | UNIT TESTED（成功） |
| PostgreSQL 真库 E2E | **NOT VERIFIED**（本机无 PostgreSQL/Docker） |
| 浏览器真实 E2E | **NOT VERIFIED**（未跑） |
| 跨设备/离线/双用户隔离 | **NOT VERIFIED**（需真 PG + 双浏览器） |

## 二、本次（v12repair3）完成项

1. **Domain → Sync Queue adapters**（`src/lib/sync/adapters.ts`）
   - 提供 `enqueueXpEvent / enqueueSession / enqueueWordbook / enqueueReviewItem / enqueueDailyPlan / enqueueProfile / enqueuePreferences / enqueueSettings / enqueueTranslationHistory / enqueueWritingHistory`
   - Guest 未登录自动 no-op；登录后才入队
   - 业务层在本地写成功后调用即可，不需要在页面组件里散落 enqueue

2. **Queue 真正 user-scoped**（`src/lib/sync/client.ts`）
   - queue key 含当前 userId：`cet-daily:v12:sync-queue:<userId>`
   - User A 的 pending mutation 不会在 User B 登录后被当 B 的 push
   - Guest 仍用 legacy key

3. **账号切换 Provider reset**（`src/components/providers.tsx`）
   - `AccountResetGate` 用 `key={userId || 'guest'}` 重挂整棵 Provider 树
   - Guest→A→B→A 每次切换都是新内存状态，B 看不到 A 的 state
   - 不依赖 `window.location.reload()`

4. **Streak 跨设备一致**
   - 已确认 `src/lib/profile/stats.ts` 中 streak 由 `completedDays` + `completedSessions` 派生，不是单独存储
   - 只要 sync session（含 day 字段）+ dailyPlan 就够，streak 自动一致
   - 规则：完成今日总关卡才计、漏 1 天保留、连续漏 2 天清零（现有逻辑未改）

5. **Lint 清零**
   - 删除未使用的 `applyMutation`
   - `npm run lint` 0 error 0 warning

6. **Pull → merge → hydrate 顺序**
   - SyncProvider：登录后先 push pending → 再 pull → hydrate
   - 恢复 online 事件自动触发同一流程

## 三、验证结果

| 命令 | 结果 |
|---|---|
| `npm run typecheck` | 0 error |
| `npm run lint` | 0 error 0 warning |
| `npm test` | **314/314 通过** |
| `npm run build` | 成功 |
| `npx prisma validate` | 未跑（prisma CLI 安装损坏过） |
| `npx prisma generate` | 未跑（同上） |
| `prisma migrate deploy` | **NOT VERIFIED**（无 PG） |

## 四、未完成 / NOT VERIFIED

### 必须有真 PostgreSQL 才能跑的（§13-24）
- 在全新空 PG 上 `migrate deploy` 从 0 建表 + 再次 deploy 幂等
- Register/Login/Logout/Duplicate email/Wrong password/Rate limit/Unauthenticated sync 真实 HTTP
- User A vs User B 数据隔离 + 伪造 userId 越权
- Guest→Account migration（"稍后处理"不污染云 / 合并不重复）
- 双 Browser Context：Device A 学习 → Device B 同账号恢复 Profile/XP/Streak/Wordbook/Review/DailyPlan
- 离线 → 在线自动 push/pull/hydrate
- 双设备 XP 并发（A +10 / B +15 → 总 25，不重不漏）
- DailyPlan 双端 completed 并集
- Wordbook 并集 + tombstone 后 re-add
- Review version-aware 在真库上覆盖旧 version

### 代码层仍未做
- 现有业务 Provider（VocabularyProvider/ReviewProvider/DailyPlanProvider 等）的 mutation 点**还没调用** adapters 的 enqueue 函数。当前 adapter 只是"可用"，没有"被接进"。
- 统一 StorageContext hook 未做（现有 Provider 仍直接读 `cet-daily:*` legacy key；hydrate 写 user namespace 但 Provider 不感知）
- 业务 Provider 没有根据 scope 切换读 key

## 五、v12repair3 §29 必答题

| # | 问题 | 回答 |
|---|---|---|
| 1 | Guest 不登录是否完整可学？ | **YES**（guest 走 legacy localStorage，V4–V11 行为不变） |
| 2 | Guest→Cloud 是否真实 PG E2E？ | **NO**（代码就绪，真库未跑） |
| 3 | Migration 是否真实 DB 幂等？ | **NO**（手写 SQL 与 schema 对齐，未在真 PG 执行） |
| 4 | 同账号跨设备是否恢复核心数据？ | **PARTIAL**（hydrate 写 localStorage，UI 重挂后读；未在真双浏览器验证） |
| 5 | 离线→在线是否真实同步？ | **PARTIAL**（online 事件 + push 队列就绪；未真网络中断验证） |
| 6 | 双设备 XP 是否不丢不重？ | **PARTIAL**（P2002 幂等 + eventId 唯一索引就绪；未真并发验证） |
| 7 | Wordbook/Review/DailyPlan 冲突是否真实验证？ | **PARTIAL**（merge policy 单测覆盖；真库未跑） |
| 8 | User A 是否无法访问 User B？ | **PARTIAL**（服务端从 session 取 userId，忽略 body userId；未伪造 userId E2E） |
| 9 | Account switching 是否没有内存/LocalStorage 串号？ | **YES**（key={userId} 重挂 + queue key 按 userId 分） |
| 10 | Streak 是否跨设备一致？ | **PARTIAL**（streak 由 sessions 派生，同步即一致；未双设备验证） |
| 11 | V4–V11 是否正常且旧数据无需清空？ | **YES**（guest 仍读 legacy key；314 回归测试全过） |
| 12 | Phone/WeChat 未来是否不用重写学习业务层？ | **PARTIAL**（adapters 已抽象，但业务 Provider 尚未真正调用） |

## 六、结论

**V12 STILL INCOMPLETE**

原因：
1. 本机无 PostgreSQL/Docker，所有真库 E2E（§13–24）无法执行
2. 业务 Provider 尚未真正调用 adapters（"可用"≠"已接"）
3. 统一 StorageContext 未落地

代码层骨架和隔离机制已就绪，但按 v12repair3 §28 的标准，不能自称为 READY。

## 七、Git 状态

- 本地 commit 已完成（message: `fix: finish V12 domain sync and account isolation`）
- **push 失败**：本机到 github.com:443 连续 3 次 connection reset / timeout
- 网络恢复后执行 `git push origin feature/v12-cloud-sync` 即可
- 未 merge main / 未打 tag / 未建 Release
